@model IEnumerable<GetAboutsDto>

@{
    int rowNumber = 1;
}


<div class="container-fluid">
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">About List</h4>
                    <p class="card-subtitle mb-4">
                        Manage Abouts here. You can create, update and delete abouts.
                    </p>
                    <div class="mb-2">
                        <a asp-area="Admin" asp-controller="About" asp-action="Create" class="btn btn-success waves-effect waves-light">
                            <i class="fas fa-plus"></i>
                            Create About
                        </a>
                    </div>
                    @if (!Model.Any())
                    {
                        <div class="alert alert-info">
                            There is no about here. Please create a new one.
                        </div>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-striped mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>ImageUrl</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <th scope="row">@(rowNumber++)</th>
                                            <td>@item.Title</td>
                                            <td>@item.Description</td>
                                            <td>
                                                @item.ImageUrl
                                                <a target="_blank" href="~/@item.ImageUrl" class="btn btn-primary btn-rounded text-white">
                                                    <i class="fas fa-image"></i>
                                                    View Image
                                                </a>
                                            </td>
                                            <td>
                                                <a asp-area="Admin" asp-controller="About" asp-action="Update" asp-route-id="@item.Id" class="btn btn-warning text-white">
                                                    <i class="bi bi-pencil-fill"></i>
                                                    Edit
                                                </a>
                                                <a asp-area="Admin" asp-controller="About" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger text-white">
                                                    <i class="bi bi-trash3-fill"></i>
                                                    Remove
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</div>
