@model List<GetBannersDto>

<div class="container-fluid">
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Banner List</h4>
                    <p class="card-subtitle mb-4">
                        Manage car features efficiently. Add, edit, or remove features like sunroof, leather seats, or parking sensors using the provided actions.
                    </p>
                    <div class="mb-2">
                        <a asp-area="Admin" asp-controller="Banner" asp-action="Create" class="btn btn-success waves-effect waves-light">
                            <i class="fas fa-plus"></i>
                            Create Banner
                        </a>
                    </div>
                    @if (!Model.Any())
                    {
                        <div class="alert alert-info">
                            There is no banners here. Please create a new one.
                        </div>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-striped mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Video Description</th>
                                        <th>Video Url</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @for (int i = 0; i < Model.Count; ++i)
                                    {
                                        <tr>
                                            <td scope="row">@(i + 1)</td>
                                            <td>@Model[i].Title</td>
                                            <td>@Model[i].Description</td>
                                            <td>@Model[i].VideoDescription</td>
                                            <td>
                                                @Model[i].VideoUrl
                                                <a target="_blank" href="@Model[i].VideoUrl" class="btn btn-primary btn-rounded text-white">
                                                    <i class="fas fa-play"></i>
                                                    Watch Video
                                                </a>
                                            </td>
                                            <td>
                                                <a asp-area="Admin" asp-controller="Banner" asp-action="Update" asp-route-id="@Model[i].Id" class="btn btn-warning text-white">
                                                    <i class="bi bi-pencil-fill"></i>
                                                    Edit
                                                </a>

                                                <a asp-area="Admin" asp-controller="Banner" asp-action="Delete" asp-route-id="@Model[i].Id" class="btn btn-danger text-white">
                                                    <i class="bi bi-trash3-fill"></i>
                                                    Remove
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</div>
